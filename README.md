# Scripts
GA-Unpacker is an offline, dependency-free tool that decodes Google Authenticator migration links. It extracts TOTP secrets, names, issuers, and account settings by parsing the encoded protobuf payload. Ideal for securely migrating, backing up, or auditing your 2FA accounts.
